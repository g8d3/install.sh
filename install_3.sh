# TODO install hub

curl -L https://github.com/github/hub/releases/download/v2.2.1/hub-linux-amd64-2.2.1.tar.gz -o hub.tar.gz
sudo tar -xvf hub.tar -C /opt
sudo ln -s /opt/hub-linux-amd64-2.2.1/hub /bin

# generate ssh key

ssh-keygen -t rsa -q -N ""

# add ssh key to github

# elastic search

# autosave sublime
# install packages sublime
